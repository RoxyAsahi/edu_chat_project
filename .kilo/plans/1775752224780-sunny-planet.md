# UI Optimization Plan

1. **Top Right Window Buttons Optimization**
   - File: `src/renderer/index.html`
   - Modify the minimize, maximize, and close buttons (`#minimize-btn`, `#maximize-btn`, `#close-btn`) to use proper Material Symbols Outlined icons instead of basic text characters (`_`, `▢`, `×`).
   - The new icons will be `<span class="material-symbols-outlined" style="font-size: 16px;">remove</span>`, `<span class="material-symbols-outlined" style="font-size: 14px;">crop_square</span>`, and `<span class="material-symbols-outlined" style="font-size: 18px;">close</span>`.

2. **Translate UI to Chinese**
   - File: `src/renderer/index.html`
   - Translate all the visible English texts in the sidebar, panels, settings, and main chat stage to Chinese. Examples: 
     - "Multi-agent single chat workspace" -> "多智能体单页聊天工作区"
     - "Assistants" -> "助手", "Agents" -> "智能体", "New" -> "新建"
     - "History" -> "历史", "Topics" -> "话题"
     - "Current Chat" -> "当前聊天", "Select an agent" -> "请选择一个智能体"
     - "Settings" -> "设置", "Configuration" -> "配置", "Delete" -> "删除"
     - "Profile" -> "资料", "Name" -> "名称", "Model" -> "模型"
     - "System Prompt" -> "系统提示词", "Parameters" -> "参数", "Constraints" -> "约束"
     - "Chat Styling" -> "聊天样式", "Global Connection" -> "全局连接", "Rendering" -> "渲染", "Theme" -> "主题"
   - File: `src/renderer/renderer.js`
   - Translate the dynamically set UI texts to Chinese:
     - "Select an agent" -> "请选择一个智能体"
     - "New Agent" -> "新建智能体"
     - "New Topic" -> "新建话题"
     - "Use the \"New Agent\" button to create one..." -> "使用“新建”按钮创建一个智能体，或让 Lite 在首次启动时导入您的现有数据。"

3. **Reduce Vertical Height of CurrentChat Titlebar**
   - File: `src/renderer/style.css`
   - Target `.chat-stage__header` and change its padding from `20px 24px` to `10px 24px` to effectively reduce the vertical space it occupies, making it narrower as requested.